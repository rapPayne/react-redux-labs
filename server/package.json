{
  "name": "rest-api-server",
  "version": "1.1.0",
  "description": "For serving data for labs and demos via an API",
  "scripts": {
    "reload-daam": "node ./reload-daam.js",
    "server": "json-server --config json-server.json ./database.json"
  },
  "author": "RapPayne",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.1.0",
    "json-server": "^0.16.3",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  },
  "engines": {
    "node": "^14.0.0"
  }
}
