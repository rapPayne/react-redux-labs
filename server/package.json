{
  "name": "rest-api-server",
  "version": "0.0.1",
  "description": "For serving data for labs and demos via an API",
  "main": "index.js",
  "scripts": {
    "reload-daam": "node ./reload-daam.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --port 3007 --routes ./routes.json --middlewares ./middlewares.js --watch ./database.json",
    "devserver": "nodemon ./daam-server"
  },
  "author": "Rap Payne",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "faker": "^4.1.0",
    "json-server": "^0.15.1",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
