# See https://help.github.com/ignore-files/ for more about ignoring files.
# Temporary. Remove these next lines when needed
**/store_old
solutions/**/working/

# to NOT share with the world
**/sensitive_constants.js
toCopy/

# no need to save the DB; it should be regenerated by each user
**/database.json

# dependencies
**/node_modules

# Don't need to save configuration and removing these will keep dependabot from flagging security issues (which we don't care about since risk is nonexistent).
**/package-lock.json
**/yarn.lock

# testing
**/coverage

# production
**/build

# React Native
**/.expo/*
**/.expo-shared/*
**/*.jks
**/*.p8
**/*.p12
**/*.key
**/*.mobileprovision
# **/*.orig.*
**/web-build/

# expo
**/.expo
**/.watchmanconfig

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

**/npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE files
/.idea
/.vscode