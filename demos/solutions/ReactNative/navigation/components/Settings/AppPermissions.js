import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const AppPermissions = () => {
  return (
    <>
    <Text>Set permissions on this scene</Text>
    </>
  );
}

const styles = StyleSheet.create({
  foo: {
  },
});
