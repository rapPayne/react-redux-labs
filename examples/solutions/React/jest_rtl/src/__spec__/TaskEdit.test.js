import { TaskEdit } from '../Task';

describe("TaskEdit component", () => {
  it("can add a Task", () => { });

  it("can mark a Task as completed", () => { });

  it("can mark a Task as un-completed", () => { });

  it("can change the description of a Task", () => { });

  it("can change the priority of a Task", () => { });

  it("can change the date of a Task", () => { });

});